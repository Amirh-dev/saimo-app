mutation CreateGoal($input: CreateGoalInput!) {
    createGoal(input: $input) {
        id
        createdAt
        updatedAt
        title
        note
        targetDate
        status
        userID
    }
}
