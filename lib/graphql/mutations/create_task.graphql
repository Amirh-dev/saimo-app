mutation CreateTask($input: CreateTaskInput!) {
  createTask(input: $input) {
    id
    title
    shortDescription
    note
    type
    date
    durationM
    hasReminder
    reminderTime
    recurringDays
    status
    tags {
      id
      name
    }
  }
}
